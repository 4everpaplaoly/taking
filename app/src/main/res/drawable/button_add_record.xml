<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 배경 색상 -->
    <solid android:color="#F08080" />

    <!-- 모서리 둥글게 -->
    <corners android:radius="20dp" />

    <!-- 그림자 느낌을 위한 약간의 패딩(선택) -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp"/>
</shape>
